import { LaunchForm } from "../../components/LaunchForm";

export const metadata = {
  title: "Launch — AgentLaunch",
  description: "Deploy AI agents, tokens, and skill modules on BNB Chain in seconds.",
};

export default function LaunchPage() {
  return <LaunchForm />;
}
